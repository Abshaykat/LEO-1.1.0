[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "leo-1-1-0-python"
version = "0.1.0"
description = "Pure Python governed runtime foundation for L.E.O. 1.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["leo_python/tests"]
pythonpath = ["."]
